  <%= form_tag(searches_result_path, method: "get") do %> <%# method를 get방식으로 받아내며, parameter가 /searches/result로 넘어갈 예정. %>
    <div class="input-group">
      <%= text_field_tag :search_text, nil, placeholder: "레시피를 검색 하세요.", class: "form-control"%>
      <%= hidden_field_tag(:bulletin, "#{post}") %> <%# 모델 이름을 자동으로 받아냄 (name은 [bulletin], value 값에는 [모델 이름]이 들어감) %>
      <div class="input-group-btn">
        <%= button_tag(type: "submit", class: "btn btn-default") do %>
          <i class="fas fa-search"></i>
        <% end %>
      </div>
    </div>
  <% end %>
